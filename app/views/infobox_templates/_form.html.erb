<%= form_for(@infobox_template) do |f| %>
  <% if @infobox_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@infobox_template.errors.count, "error") %> prohibited this infobox_template from being saved:</h2>

      <ul>
      <% @infobox_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_area :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
